PORT=3000
MONGODB_URI=mongodb://127.0.0.1:27017/solar_generation_api
NODE_ENV=development
# Required when authentication is implemented; use a long random secret.
JWT_SECRET=
JWT_EXPIRES_IN=1h
# Used only by `npm run seed:users`; never commit the real value.
SEED_USER_PASSWORD=
